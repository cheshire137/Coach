<h1>Editing content_type</h1>

<% form_for(@content_type) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @content_type %> |
<%= link_to 'Back', content_types_path %>